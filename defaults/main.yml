---
# defaults file for nutanix-ansible-galaxy-role-lcm
  validate_certs: no
  lcm_run_inventory: True
  lcm_run_software_updates: True
  lcm_software_to_update: []
  lcm_run_firmware_updates: True
  lcm_run_precheck: True
  lcm_retries: 5
  lcm_delay: 300
